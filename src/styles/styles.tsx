import { extendTheme } from '@chakra-ui/react';

const theme: any = extendTheme({
    colors: {
        mainBg: '#F6921D',
        mainBorder: '#B14623',
        modalBg: '#FFFFFF',
        popupBg: '#FFFFFF',
        inputBg: '#EDEDED',
    }
});

export {
    theme,
};
